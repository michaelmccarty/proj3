function executeRound(pokemon1, pokemon2, action1, action2, combatant1, combatant2) {
    // returns modified pokemon1, pokemon2
    // also needs to return the 'script' for the turn.
    // script is a series of objects containing who used what move, if it hit, if it crit, and how effective it was 
}